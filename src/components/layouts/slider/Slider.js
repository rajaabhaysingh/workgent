import React from "react";

import Carousel from "react-multi-carousel";
import "react-multi-carousel/lib/styles.css";

const Slider = ({ component, heading }) => {
  return <div className="">{component}</div>;
};

export default Slider;
