import React from "react";

const Fallback = () => {
  return (
    <div>
      <i className="fas fa-compact-disc fa-spin fsxs fc_pri h-100 w-100 fcc"></i>
    </div>
  );
};

export default Fallback;
